# EasePi-R2 / RK3588 Mali-G610 mainline GPU driver.
# The DTB now provides the panthor-compatible clocks, IRQs, OPP table,
# mali/sram supplies, and GPU power-domain supply, so load it at boot.
panthor
