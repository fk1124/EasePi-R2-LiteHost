#!/usr/bin/env bash
set -euo pipefail

if [ "$(id -u)" -ne 0 ]; then
    exec sudo /usr/local/sbin/desktop-disable "$@"
fi

systemctl set-default multi-user.target >/dev/null 2>&1 || true
echo "Desktop keeps tty as default boot target."
